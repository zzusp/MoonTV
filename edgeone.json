{
  "buildCommand": "pnpm gen:runtime && pnpm gen:manifest && next build && npx @cloudflare/next-on-pages --experimental-minify",
  "installCommand": "pnpm install --frozen-lockfile",
  "outputDirectory": "out",
  "nodeVersion": "22.11.0"
}
